params:
  - id: admin_password
    type: secret
    name: "Admin Password"
    description: "Initial admin password (at least 12 chars, must not contain $). Leave empty to auto-generate."
    default_value: ""
    optional: true

locales:
  zh:
    admin_password:
      name: "管理员密码"
      description: "管理员初始密码（至少 12 位，不能包含 $；留空自动生成）"
